# Встановлення і запуск

В завданні не дотримано вимоги стосовно версії Laravel. Проект використовує Laravel 13

## Клонувати репозиторій
```
git clone https://github.com/arakul23/wtg_spain_test

cd wtg_spain_test

docker compose up --build -d

docker compose exec php php artisan key:generate

docker compose exec php php artisan migrate --seed
```
Команди `docker compose exec php php artisan key:generate` і `docker compose exec php php artisan migrate --seed` можуть не виконатись одразу після збірки контейнерів, можливо потрібно буде зачекати 5-10 секунд поки виконається `composer install`

Після цього API доступне на http://localhost:8080.

В проекті є два json файли з даними для зручного імпорту великої кількості записів (import_supplier_a_10.json, import_supplier_b_10.json). Вони не використовуються в коді, а додані щоб можно було скопіювати і вставити у запит (наприклад через Postman).

Команда `php artisan queue:work --tries=3 --timeout=90` запускається в окремому контейнері, тому вручну виконувати не треба (на проді краще налаштовувати такі речі через supervisor)

Черги працюють через БД (Mysql).

Тести використовують окрему тестову БД (DB_DATABASE з phpunit.xml).

# Ідемпотентність імпорту

Імпорт побудований так, щоб повторне надсилання того самого запиту ніколи не створювало дублікатів і не запускало обробку вдруге.

Ключ ідемпотентності — унікальна пара (supplier_id, external_import_id) на рівні БД.

# Алгоритм у ImportService::handle()

Шукаємо існуючий Import за парою `supplier_id, external_import_id` через `firstOrCreate()`.
Якщо запис вже існував — просто повертаємо його без додаткових операцій, такий самий запит не буде впливати на систему. 
Якщо запис новий — диспатчимо ProcessImportJob.
Якщо два ідентичні запити прийдуть одночасно, обидва можуть пройти перевірку first() до того, як перший встигне закомітити create(). У такому разі другий запит зловить QueryException від БД і дочитає вже створений запис замість того, щоб повернути помилку.

Окремо від ідемпотентності імпорту — правило оновлення офера, якщо `supplier_id, offer.external_id` вже існує в іншому імпорті, то `Offer::updateOrCreate()` всередині `ProcessImportJob` оновлює дані існуючого офера замість створення дубліката


# Блокування при резервації
При резервації ми блокуємо запис `offer` за допомогою `lockForUpdate()` всередині транзакції. Якщо в цей час паралельно надійде ще один запит на резервацію цього ж `offer`, він буде чекати завершення поточної транзакції та зняття блокування.

Після перевірки доступності виконується `$locked->decrement('available_units')`. Тому другий запит побачить вже актуальне значення `available_units`. Якщо доступних одиниць більше немає (`available_units < 1`), резервація не буде створена.